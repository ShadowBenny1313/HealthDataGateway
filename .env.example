# HealthData Gateway Environment Configuration Example
# Replace with actual values and rename to .env for deployment

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
DEBUG_MODE=false

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=healthdata
DB_USER=dbuser
# SECURITY NOTE: Never commit actual passwords to source control
DB_PASSWORD=replace_with_secure_password

# JWT Authentication
# SECURITY NOTE: Generate a unique, strong key and store securely
JWT_SECRET_KEY=generate_unique_secure_key_here
JWT_EXPIRE_MINUTES=60

# Blockchain Configuration
# SECURITY NOTE: Store private keys in a secure vault or key management service
# ETH_PRIVATE_KEY=your_private_key_here
BLOCKCHAIN_PROVIDER=http://localhost:8545
CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000

# Security settings
CORS_ORIGINS=http://localhost:3000,https://your-production-domain.com
